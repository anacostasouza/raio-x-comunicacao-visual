rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Permite qualquer leitura e escrita em qualquer documento
    match /{document=**} {
      allow read, write: if true;
    }

  }
}
